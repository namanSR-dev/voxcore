[project]
name = "voxcore"
version = "0.1.0"
description = "Open-source runtime for building real-time conversational voice AI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pyright>=1.1.411",
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]
